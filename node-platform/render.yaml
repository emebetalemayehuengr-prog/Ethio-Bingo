services:
  - type: web
    name: 40bingo-api
    runtime: node
    plan: starter
    rootDir: node-platform
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: 40bingo-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: 40bingo-redis
          property: connectionString

  - type: redis
    name: 40bingo-redis
    plan: starter
    ipAllowList: []

databases:
  - name: 40bingo-db
    plan: starter
